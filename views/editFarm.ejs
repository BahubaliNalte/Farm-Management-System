<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Farm Listing</title>
  <link rel="stylesheet" href="/css/register.css">
</head>
<body>
  <div class="register-container">
    <h2>Edit Farm Listing</h2>
    <form action="/farms/edit/<%= farm._id %>" method="POST">
      <input type="text" name="location" placeholder="Location" value="<%= farm.location %>" required>
      <input type="number" name="size" placeholder="Size (in acres)" value="<%= farm.size %>" required>
      <input type="text" name="soilType" placeholder="Soil Type" value="<%= farm.soilType %>" required>
      <input type="text" name="amenities" placeholder="Amenities (comma separated)" value="<%= farm.amenities.join(', ') %>" required>
      <button type="submit">Update</button>
    </form>
    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>
  </div>
</body>
</html>